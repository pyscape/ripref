# ripref
Keep documentation in sync with code, for any language. ripref replaces fragile line-number citations with semantic anchors (symbols, decisions, headings)  that survive refactoring. Subcommands: create (index), read (dereference), search (citations), enforce (drift detection).
